import{_ as t,c as n,o as a,ag as r}from"./chunks/framework.CoPVQkzc.js";const p=JSON.parse('{"title":"Threading and Ordering","description":"Which thread processes a message, what runs in parallel, what order survives, and how the response to the sender is chosen","frontmatter":{"title":"Threading and Ordering","description":"Which thread processes a message, what runs in parallel, what order survives, and how the response to the sender is chosen"},"headers":[],"relativePath":"engine/threading_and_ordering.md","filePath":"engine/threading_and_ordering.md","lastUpdated":1788476353000}'),s={name:"engine/threading_and_ordering.md"};function o(i,e,h,d,c,l){return a(),n("div",null,e[0]||(e[0]=[r('<h1 id="threading-and-ordering" tabindex="-1">Threading and Ordering <a class="header-anchor" href="#threading-and-ordering" aria-label="Permalink to &quot;Threading and Ordering&quot;">​</a></h1><p>There is no central pool of worker threads in the engine. The thread that receives a message is the thread that processes it: it runs the preprocessor, the source filter and transformer, the destination chains, and the postprocessor, and it returns the response to the sender. Parallelism comes from three places, each with its own setting: how many receiving threads may be inside a channel at once, how the destinations are grouped into chains, and how many threads drain each destination queue.</p><p>Each of those settings trades order for throughput, and the engine is explicit about what it stops promising when. This page describes the mechanism behind each one, what a destination can see from the destinations before it, how the response to the sender is selected, and what the channel&#39;s threads are called in a thread dump.</p><h2 id="max-processing-threads" tabindex="-1">Max Processing Threads <a class="header-anchor" href="#max-processing-threads" aria-label="Permalink to &quot;Max Processing Threads&quot;">​</a></h2><p><strong>Max Processing Threads</strong> on the Source tab is a limit, not a pool. The engine holds a fair semaphore with that many permits, and every message acquires one on the way in and releases it when the response has gone back. With the default of 1, a channel processes one message at a time and the sender of the next waits. With a higher value, that many receiving threads run through the channel at the same time.</p><p>Order is kept only at the door. Messages acquire permits in arrival order and then run concurrently, so they complete, send, and respond in whatever order they finish. The setting&#39;s own tooltip says that message order is not guaranteed above 1.</p><p>With the source queue on, the same number also sets how many source queue threads drain the queue. One connector is different: the Web Service Listener sizes a real thread pool from this value, four larger than the setting.</p><p>The permit count is fixed when the channel is deployed.</p><h2 id="destination-chains" tabindex="-1">Destination chains <a class="header-anchor" href="#destination-chains" aria-label="Permalink to &quot;Destination chains&quot;">​</a></h2><p>Each destination has a <strong>Wait for previous destination</strong> checkbox. It is checked by default and has no effect on the first destination. Every destination with the box unchecked starts a new chain; every destination with it checked joins the chain of the destination above it. The Destinations table shows the resulting chain number in its <strong>Chain</strong> column.</p><p>Chains run at the same time. Within a chain, destinations run one after another, and the next destination&#39;s connector message is not created until the previous one has finished. The engine hands all chains but the last to a per-channel thread pool and runs the last chain on the receiving thread itself, then waits for every chain before moving on to the postprocessor.</p><p>A destination that filters the message does not stop its chain; the next destination still runs. A destination whose filter or transformer errors, or that fails with a runtime exception, stops its chain, and the destinations after it are not processed for that message.</p><h3 id="what-a-later-destination-sees" tabindex="-1">What a later destination sees <a class="header-anchor" href="#what-a-later-destination-sees" aria-label="Permalink to &quot;What a later destination sees&quot;">​</a></h3><p>When the engine creates the connector message for the next destination in a chain, it gives it:</p><table tabindex="0"><thead><tr><th>Data</th><th>What the next destination gets</th></tr></thead><tbody><tr><td>Source map</td><td>The same map, shared by every connector message. It is read-only</td></tr><tr><td>Channel map</td><td>A copy of the previous destination&#39;s channel map, including anything its transformer added</td></tr><tr><td>Response map</td><td>A copy of the previous destination&#39;s response map, including that destination&#39;s own response under its <code>d</code> key, for instance <code>d1</code></td></tr><tr><td>Raw content</td><td>The source&#39;s Encoded content, the same starting point every destination gets</td></tr></tbody></table><p>Because these are copies, nothing a later destination writes reaches an earlier one, and nothing crosses between chains while they run. The first destination of every chain starts from copies of the source connector message&#39;s maps.</p><p>After the chains finish, the postprocessor sees a merged connector message built by sorting all connector messages by chain and then by position within the chain and merging their maps in that order. The merged result is the same regardless of which chain finished first.</p><h2 id="responding-to-the-sender" tabindex="-1">Responding to the sender <a class="header-anchor" href="#responding-to-the-sender" aria-label="Permalink to &quot;Responding to the sender&quot;">​</a></h2><p>The <strong>Response</strong> setting on the Source tab chooses what goes back to the system that sent the message.</p><table tabindex="0"><thead><tr><th>Option</th><th>Response</th></tr></thead><tbody><tr><td><strong>None</strong></td><td>Nothing is returned</td></tr><tr><td><strong>Auto-generate (Before processing)</strong></td><td>An acknowledgement generated from the raw message, before any processing</td></tr><tr><td><strong>Auto-generate (After source transformer)</strong></td><td>An acknowledgement whose status reflects the source filter and transformer</td></tr><tr><td><strong>Auto-generate (Destinations completed)</strong></td><td>An acknowledgement whose status reflects the destinations, resolved in the order ERROR, then QUEUED, then SENT, then FILTERED. One errored destination makes the response an error; one queued destination makes it queued. If a chain never ran, the response is an error</td></tr><tr><td><strong>Postprocessor</strong></td><td>Whatever the postprocessor script returned</td></tr><tr><td>A destination</td><td>That destination&#39;s response. Stored as the destination&#39;s <code>d</code> key rather than its name</td></tr></tbody></table><p>The response is selected after the postprocessor, once every chain has finished, and the source connector then delivers it. With the source queue on, only <strong>None</strong> and <strong>Auto-generate (Before processing)</strong> are offered, because the response leaves before anything else has run. See <a href="./queueing.html">Queueing</a>.</p><h2 id="batch-messages" tabindex="-1">Batch messages <a class="header-anchor" href="#batch-messages" aria-label="Permalink to &quot;Batch messages&quot;">​</a></h2><p>Messages produced from one payload by the batch processor are dispatched one after another on the receiving thread, each finishing before the next is read, whatever the value of Max Processing Threads. Separate payloads can be in progress at the same time; there is no limit on concurrent batches. With the source queue on, batch messages are only enqueued in order and are then processed by the source queue threads.</p><h2 id="destination-queue-threads" tabindex="-1">Destination queue threads <a class="header-anchor" href="#destination-queue-threads" aria-label="Permalink to &quot;Destination queue threads&quot;">​</a></h2><p>A destination with queueing on sends from its own queue threads rather than from the receiving thread. <strong>Queue Threads</strong> and the <strong>Thread Assignment Variable</strong> decide how many and in what order; <a href="./queueing.html">Queueing</a> covers them. The threads that can be sending for one destination at any moment are:</p><table tabindex="0"><thead><tr><th>Queue mode</th><th>Sending threads</th></tr></thead><tbody><tr><td>Never</td><td>Up to Max Processing Threads</td></tr><tr><td>Always</td><td>Up to Queue Threads</td></tr><tr><td>On Failure</td><td>Up to the larger of the two</td></tr></tbody></table><h2 id="thread-names" tabindex="-1">Thread names <a class="header-anchor" href="#thread-names" aria-label="Permalink to &quot;Thread names&quot;">​</a></h2><p>The engine names its threads after the channel and connector they are working for, which makes a thread dump readable without a debugger. The <code>&lt;</code> suffix marks a borrowed thread that is renamed back when the task finishes.</p><table tabindex="0"><thead><tr><th>Thread name</th><th>What it is doing</th></tr></thead><tbody><tr><td><code>Channel Dispatch Thread on &lt;channel&gt; (&lt;id&gt;) &lt; &lt;original name&gt;</code></td><td>A receiving thread processing a message</td></tr><tr><td><code>Source Queue Thread &lt;n&gt; on &lt;channel&gt; (&lt;id&gt;)</code></td><td>Draining the source queue</td></tr><tr><td><code>Destination Chain Thread &lt;n&gt; on &lt;channel&gt; (&lt;id&gt;)</code></td><td>Running a destination chain from the pool</td></tr><tr><td><code>&lt;connector type&gt; Process Thread on &lt;channel&gt; (&lt;id&gt;), &lt;destination&gt; (&lt;metadata id&gt;)</code></td><td>Inside one destination&#39;s send</td></tr><tr><td><code>Destination Queue Thread &lt;n&gt; on &lt;channel&gt; (&lt;id&gt;), &lt;destination&gt; (&lt;metadata id&gt;)</code></td><td>Draining a destination queue</td></tr><tr><td><code>Recovery Task Thread on &lt;channel&gt; (&lt;id&gt;)</code></td><td>Recovering messages during start</td></tr><tr><td><code>Channel DeployTask Thread on (&lt;id&gt;)</code>, and the same for Undeploy, Start, Stop, and Halt</td><td>A lifecycle task for the channel</td></tr><tr><td><code>&lt;connector type&gt; Polling Thread on &lt;channel&gt; (&lt;id&gt;)</code></td><td>A polling source connector&#39;s scheduled poll</td></tr><tr><td><code>TCP Receiver Thread on ...</code>, <code>HTTP Receiver Thread on ...</code>, and the same for JMS, Web Service, and DICOM</td><td>A listening source connector waiting for input</td></tr><tr><td><code>Statistics Updater Thread</code></td><td>The server-wide statistics writer</td></tr></tbody></table><h2 id="where-the-threads-live" tabindex="-1">Where the threads live <a class="header-anchor" href="#where-the-threads-live" aria-label="Permalink to &quot;Where the threads live&quot;">​</a></h2><p>Every pool is per channel. The chain pool is created when the channel starts and shut down when it stops. Lifecycle tasks such as deploy, start, and stop run on one thread per channel, so tasks for the same channel run one after another while different channels proceed in parallel. There is no server-wide pool for message processing. The one server-wide limit is on removing all messages, which no more than two channels can be doing at once.</p>',31)]))}const u=t(s,[["render",o]]);export{p as __pageData,u as default};
