import{_ as t,c as s,o as a,ag as r}from"./chunks/framework.CoPVQkzc.js";const c=JSON.parse('{"title":"Queueing","description":"How the source queue and destination queues work, what QUEUED means, and what ordering the engine promises","frontmatter":{"title":"Queueing","description":"How the source queue and destination queues work, what QUEUED means, and what ordering the engine promises"},"headers":[],"relativePath":"engine/queueing.md","filePath":"engine/queueing.md","lastUpdated":1788476353000}'),n={name:"engine/queueing.md"};function o(d,e,i,h,u,l){return a(),s("div",null,e[0]||(e[0]=[r('<h1 id="queueing" tabindex="-1">Queueing <a class="header-anchor" href="#queueing" aria-label="Permalink to &quot;Queueing&quot;">​</a></h1><p>A queue in OIE is not a structure that holds messages in memory. It is a question the engine asks the database: which of this connector&#39;s messages are still waiting? The source queue is the set of source connector messages in RECEIVED status for this server. A destination queue is the set of that destination&#39;s connector messages in QUEUED status. Each queue keeps a small in-memory buffer, 1000 rows by default, filled from that query in message id order. The Queued figure on the Dashboard is the row count in the database, not the size of the buffer.</p><p>Because the queue is the database, nothing is lost when a channel stops, is undeployed, or the server restarts. The rows stay where they are and the buffer is rebuilt the next time the queue starts. This page explains how a message gets into each queue, what the settings do, what order the engine promises, and how the queues behave through the channel&#39;s states.</p><h2 id="the-source-queue" tabindex="-1">The source queue <a class="header-anchor" href="#the-source-queue" aria-label="Permalink to &quot;The source queue&quot;">​</a></h2><p>The setting is on the <strong>Source</strong> tab under Source Settings, labelled <strong>Source Queue</strong>, with two options.</p><table tabindex="0"><thead><tr><th>Option</th><th>What happens to a message</th></tr></thead><tbody><tr><td><strong>OFF (Respond after processing)</strong></td><td>The thread that received the message runs the whole channel: preprocessor, source filter and transformer, every destination chain, postprocessor. The sender waits for all of it, and its response can come from any point in that path.</td></tr><tr><td><strong>ON (Respond before processing)</strong></td><td>The engine writes the raw message, responds to the sender at once, and processes the message later on a source queue thread.</td></tr></tbody></table><p>With the queue on, the sender&#39;s response is limited to <strong>None</strong> or <strong>Auto-generate (Before processing)</strong>, because nothing else exists yet when the response goes back. The Administrator restricts the Response list accordingly.</p><p>The number of source queue threads equals <strong>Max Processing Threads</strong>. With more than one, messages leave the queue in id order but are processed at the same time, so order is not preserved. See <a href="./threading_and_ordering.html">Threading and Ordering</a>.</p><p>A source queue message is recovered by the queue itself. On start, the source queue picks up every source connector message still in RECEIVED and processes it, which is also how messages that were received but never processed before a crash are handled in Development and Production storage modes.</p><p><strong>Queue Buffer Size</strong> sets the in-memory buffer for this channel and is enabled only when the queue is on. It defaults to the server setting <strong>Default Queue Buffer Size</strong>, <code>server.queuebuffersize</code>, which is 1000 unless changed. The same default applies to destination queues.</p><h2 id="destination-queues" tabindex="-1">Destination queues <a class="header-anchor" href="#destination-queues" aria-label="Permalink to &quot;Destination queues&quot;">​</a></h2><h3 id="queue-modes" tabindex="-1">Queue modes <a class="header-anchor" href="#queue-modes" aria-label="Permalink to &quot;Queue modes&quot;">​</a></h3><p><strong>Queue Messages</strong> on each destination has three options. Underneath they are two stored booleans, <code>queueEnabled</code> and <code>sendFirst</code>.</p><table tabindex="0"><thead><tr><th>Option</th><th>Behaviour</th></tr></thead><tbody><tr><td><strong>Never</strong></td><td>Queueing off. The message is sent on the processing thread. A failure, after any retries, ends in ERROR.</td></tr><tr><td><strong>On Failure</strong></td><td>Try to send on the processing thread first. If that fails, or if the queue already holds messages, the message goes to the queue.</td></tr><tr><td><strong>Always</strong></td><td>The message goes to the queue at once and is sent by a queue thread.</td></tr></tbody></table><p>Two consequences of these rules matter in practice.</p><p>On Failure attempts a direct send only while the queue is empty. Once a backlog exists, every new message goes straight to the back of the queue. That is what stops a new message jumping ahead of older ones during an outage.</p><p>With Always, and with On Failure while a backlog exists, later destinations in the same chain and the postprocessor see this destination&#39;s response as QUEUED. Any script that expects to read the destination&#39;s real response must run after the send, which means the destination cannot be queued.</p><h3 id="how-a-failed-send-becomes-queued" tabindex="-1">How a failed send becomes QUEUED <a class="header-anchor" href="#how-a-failed-send-becomes-queued" aria-label="Permalink to &quot;How a failed send becomes QUEUED&quot;">​</a></h3><p>Every destination connector starts its response with the status QUEUED and changes it to SENT only when the send succeeds. When queueing is Never, the engine converts a QUEUED result to ERROR, logging that the status cannot be QUEUED while queueing is disabled. That single conversion is why the same transport failure produces ERROR on one destination and QUEUED on another: the difference is only the queue mode.</p><h3 id="retry-count-and-retry-interval" tabindex="-1">Retry Count and Retry Interval <a class="header-anchor" href="#retry-count-and-retry-interval" aria-label="Permalink to &quot;Retry Count and Retry Interval&quot;">​</a></h3><p>Two fields in Advanced Queue Settings govern the attempts made on the processing thread, before the message is queued or errored.</p><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>Effect</th></tr></thead><tbody><tr><td><strong>Retry Count Before Queue/Error</strong></td><td>0</td><td>Extra attempts made on the processing thread. With Never, exhausting them produces ERROR. With On Failure, exhausting them queues the message.</td></tr><tr><td><strong>Retry Interval (ms)</strong></td><td>10000</td><td>Wait between those attempts, and also the wait a queue thread observes between attempts on the same message.</td></tr></tbody></table><p>Once a message is in the queue, retries are unlimited. No counter applies to queue attempts and a queued message never becomes ERROR because retries ran out. It is retried until it sends or until someone removes it. A queue thread produces ERROR only in three narrow cases: the destination&#39;s connector type changed after the message was queued, the message was queued before Include Filter/Transformer was turned off and was never transformed, or the response transformer throws.</p><h3 id="queue-threads-and-the-thread-assignment-variable" tabindex="-1">Queue threads and the thread assignment variable <a class="header-anchor" href="#queue-threads-and-the-thread-assignment-variable" aria-label="Permalink to &quot;Queue threads and the thread assignment variable&quot;">​</a></h3><p><strong>Queue Threads</strong> sets how many threads send from this queue. The default is 1. Above 1, message order is not preserved unless a <strong>Thread Assignment Variable</strong> is set.</p><p>The variable names a map entry that the engine reads for each queued message, searching the response map, connector map, channel map, and source map in that order. Messages with the same value always go to the same thread, so they send in order relative to each other. The first N distinct values each get a thread of their own, where N is the thread count. Values after that are hashed onto the threads, so two unrelated values can share one. Messages that have no value for the variable all share a single thread, so they stay in order relative to each other but do not parallelise.</p><p>When <strong>Regenerate Template</strong> is off, the value is read from the sent content stored when the message was queued, not from the current channel. Changing the variable after messages have queued does not re-sort them.</p><h3 id="rotate-queue-regenerate-template-include-filter-transformer" tabindex="-1">Rotate Queue, Regenerate Template, Include Filter/Transformer <a class="header-anchor" href="#rotate-queue-regenerate-template-include-filter-transformer" aria-label="Permalink to &quot;Rotate Queue, Regenerate Template, Include Filter/Transformer&quot;">​</a></h3><p><strong>Rotate Queue</strong> moves on to the next message after a failure instead of retrying the same one. It is implemented as a window over message ids: the queue works through the ids present when the pass started, then wraps to the beginning. Messages that arrive during a pass wait for the next one. Rotation drops ordering by design; the setting&#39;s own tooltip says to leave it off when order matters.</p><p><strong>Regenerate Template</strong> rebuilds the destination&#39;s message from the current maps on every attempt, instead of resending the content built when the message was queued. <strong>Include Filter/Transformer</strong> goes one step further and re-runs the destination filter and transformer on every attempt. It requires Regenerate Template. Turning it off later forces any queued message that was never transformed to ERROR on its next attempt.</p><h3 id="what-order-the-engine-promises" tabindex="-1">What order the engine promises <a class="header-anchor" href="#what-order-the-engine-promises" aria-label="Permalink to &quot;What order the engine promises&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Configuration</th><th>Order</th></tr></thead><tbody><tr><td>One queue thread, rotate off</td><td>Strict order by message id. A message that keeps failing blocks everything behind it.</td></tr><tr><td>Several threads, no assignment variable</td><td>None. Every thread takes the next available message.</td></tr><tr><td>Several threads with an assignment variable, rotate off</td><td>In order for messages sharing a value. No order across values.</td></tr><tr><td>Rotate on, any thread count</td><td>None.</td></tr><tr><td>Source queue with Max Processing Threads above 1</td><td>None. The source queue has no assignment variable.</td></tr></tbody></table><h2 id="what-the-message-storage-mode-has-to-do-with-it" tabindex="-1">What the message storage mode has to do with it <a class="header-anchor" href="#what-the-message-storage-mode-has-to-do-with-it" aria-label="Permalink to &quot;What the message storage mode has to do with it&quot;">​</a></h2><p>A queue re-reads the message from the database later, so it needs the content that a send is built from. The engine checks this at deploy and refuses a channel whose storage mode cannot support its queues.</p><table tabindex="0"><thead><tr><th>Storage mode</th><th>Source queue</th><th>Destination queues</th></tr></thead><tbody><tr><td>Development</td><td>Allowed</td><td>Allowed</td></tr><tr><td>Production</td><td>Allowed</td><td>Allowed</td></tr><tr><td>Raw</td><td>Allowed</td><td>Refused</td></tr><tr><td>Metadata</td><td>Refused</td><td>Refused</td></tr><tr><td>Disabled</td><td>Refused</td><td>Refused</td></tr></tbody></table><p>The source queue needs the raw content stored, plus either the maps or a durable raw write. A destination queue needs the source&#39;s encoded content, the sent content, and the maps. Raw mode keeps only raw content, which is why it can carry a source queue and not a destination queue.</p><p>The Administrator refuses to save the combination first, with a warning beside the Message Storage slider that queueing must be disabled before using that mode. The deploy-time check is the backstop for channels imported through the API or CLI. See <a href="./message_lifecycle.html">Message Lifecycle and Storage</a> for what each mode keeps.</p><h2 id="queues-through-the-channel-s-states" tabindex="-1">Queues through the channel&#39;s states <a class="header-anchor" href="#queues-through-the-channel-s-states" aria-label="Permalink to &quot;Queues through the channel&#39;s states&quot;">​</a></h2><table tabindex="0"><thead><tr><th>State or action</th><th>Source queue</th><th>Destination queues</th></tr></thead><tbody><tr><td><strong>Started</strong></td><td>Draining</td><td>Draining</td></tr><tr><td><strong>Paused</strong></td><td>Draining</td><td>Draining</td></tr><tr><td><strong>Stopped</strong></td><td>Threads stopped. Rows stay in RECEIVED.</td><td>Threads stopped. Rows stay in QUEUED.</td></tr><tr><td><strong>Halt</strong></td><td>Threads interrupted, possibly mid-message</td><td>Threads interrupted, possibly mid-send</td></tr><tr><td><strong>Undeployed</strong></td><td>As Stopped</td><td>As Stopped</td></tr><tr><td>Initial state <strong>Paused</strong></td><td>Starts and drains</td><td>Start and drain</td></tr><tr><td>Initial state <strong>Stopped</strong></td><td>Does not start</td><td>Do not start</td></tr></tbody></table><p>Pause stops only the source connector, so a paused channel accepts nothing new but keeps working through both queues. Stop waits for in-flight messages to finish, then stops the queue threads. Halt does not wait; the engine&#39;s own comment on halt says that a message interrupted mid-send may or may not have been delivered, and that a duplicate is a possible consequence.</p><p>A single destination can be stopped on its own only when its queueing is enabled. While it is stopped, every message for it goes to its queue regardless of mode, and nothing sends until it is started again.</p><h2 id="consequences-worth-knowing" tabindex="-1">Consequences worth knowing <a class="header-anchor" href="#consequences-worth-knowing" aria-label="Permalink to &quot;Consequences worth knowing&quot;">​</a></h2><ul><li>The Data Pruner never removes a message that has a connector message in QUEUED, PENDING, or ERROR status. A queue that never drains grows the database until someone deals with it.</li><li>Queues are scoped to a server id. Each server processes only the rows it wrote.</li><li>Removing a queued message from the Message Browser, or removing all messages, is handled by the queue but logs an error for the interrupted attempt with the text &quot;This error is expected if the message was manually removed from the queue.&quot; Removing all messages requires the channel to be stopped.</li><li>Reducing a queue&#39;s buffer size below the current buffer contents clears the buffer; it refills from the database.</li></ul>',43)]))}const m=t(n,[["render",o]]);export{c as __pageData,m as default};
