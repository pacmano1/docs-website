import{_ as t,c as s,o,ag as n}from"./chunks/framework.CoPVQkzc.js";const p=JSON.parse('{"title":"Message Lifecycle and Storage","description":"What the engine does with a message from receipt to response, which statuses and content it records, and what each storage mode keeps","frontmatter":{"title":"Message Lifecycle and Storage","description":"What the engine does with a message from receipt to response, which statuses and content it records, and what each storage mode keeps"},"headers":[],"relativePath":"engine/message_lifecycle.md","filePath":"engine/message_lifecycle.md","lastUpdated":1788476353000}'),a={name:"engine/message_lifecycle.md"};function r(d,e,i,h,c,l){return o(),s("div",null,e[0]||(e[0]=[n('<h1 id="message-lifecycle-and-storage" tabindex="-1">Message Lifecycle and Storage <a class="header-anchor" href="#message-lifecycle-and-storage" aria-label="Permalink to &quot;Message Lifecycle and Storage&quot;">​</a></h1><p>A message in OIE is one unit of data travelling through a channel. The engine records it as one message row plus one connector message per connector it passes through: a channel with a source and two destinations writes three connector messages for every inbound message. Each connector message has its own status, its own timestamps, and its own set of stored content, which is why the Message Browser can show the raw, transformed, encoded, and sent forms of the same message side by side.</p><p>What the engine stores at each step depends on the channel&#39;s message storage mode, and what it stores determines what it can recover after a crash, what it can queue, and what the Message Browser can show. This page walks the processing path once, in order, then describes the statuses, the content types, the storage modes, and the two mechanisms that remove data.</p><h2 id="messages-and-connector-messages" tabindex="-1">Messages and connector messages <a class="header-anchor" href="#messages-and-connector-messages" aria-label="Permalink to &quot;Messages and connector messages&quot;">​</a></h2><p>A single inbound payload does not always become a single message. When the source connector&#39;s batch processor is on, one file or stream can be split into many messages, each with its own id. See <a href="#batch-processing">Batch processing</a>.</p><p>Connector messages are numbered by <strong>metadata id</strong>. The source connector is always 0. Destinations are numbered from 1 in the order they were added to the channel.</p><h2 id="what-the-database-holds" tabindex="-1">What the database holds <a class="header-anchor" href="#what-the-database-holds" aria-label="Permalink to &quot;What the database holds&quot;">​</a></h2><p>Every channel gets its own set of tables, named with a local channel id that the <code>D_CHANNELS</code> table maps to the channel&#39;s id.</p><table tabindex="0"><thead><tr><th>Table</th><th>Contents</th></tr></thead><tbody><tr><td><code>d_m</code></td><td>One row per message: server id, received date, whether processing finished, and the original and import ids for reprocessed and imported messages</td></tr><tr><td><code>d_mm</code></td><td>One row per connector message: status, connector name, received, send, and response dates, send attempts, chain and order ids</td></tr><tr><td><code>d_mc</code></td><td>Every stored content, map, and error, one row per content type per connector message</td></tr><tr><td><code>d_mcm</code></td><td>Custom metadata columns, one real database column per configured column</td></tr><tr><td><code>d_ma</code></td><td>Attachments</td></tr><tr><td><code>d_ms</code></td><td>Received, filtered, sent, and errored counts per connector, current and lifetime</td></tr><tr><td><code>d_msq</code></td><td>The sequence that issues message ids</td></tr></tbody></table><p>A destination&#39;s Raw content is never written. It is always the source&#39;s Encoded content, and the engine reads that row back when it needs it.</p><h2 id="statuses" tabindex="-1">Statuses <a class="header-anchor" href="#statuses" aria-label="Permalink to &quot;Statuses&quot;">​</a></h2><p>Each connector message carries one status. The letter is what the database stores.</p><table tabindex="0"><thead><tr><th>Status</th><th>Meaning</th><th>When it is set</th></tr></thead><tbody><tr><td><strong>RECEIVED</strong> (R)</td><td>The connector message exists and processing has not finished</td><td>On creation, for the source and for every destination</td></tr><tr><td><strong>FILTERED</strong> (F)</td><td>The connector&#39;s filter rejected the message</td><td>By the filter</td></tr><tr><td><strong>TRANSFORMED</strong> (T)</td><td>The filter accepted and the transformer completed</td><td>By the transformer. For the source connector this is the final status on the happy path; the source never becomes SENT</td></tr><tr><td><strong>QUEUED</strong> (Q)</td><td>The destination has the message and a queue thread will send it</td><td>On every destination after its transformer, before any send is attempted, and again whenever a send does not succeed with queueing on</td></tr><tr><td><strong>SENT</strong> (S)</td><td>The destination&#39;s send succeeded</td><td>Only from the connector&#39;s own response after a send</td></tr><tr><td><strong>ERROR</strong> (E)</td><td>Processing stopped with an error</td><td>Preprocessor, filter or transformer, send with queueing off, response transformer, or a runtime failure in a chain</td></tr><tr><td><strong>PENDING</strong> (P)</td><td>The destination sent the message and stored the response, but the response transformer has not finished</td><td>By the destination immediately after the response is written, only when a response transformer will run</td></tr></tbody></table><p>PENDING exists so that a crash during the response transformer is recoverable: the response is already on disk, and recovery re-runs only the response transformer.</p><p>A message is <strong>complete</strong> when every one of its connector messages is FILTERED, TRANSFORMED, or SENT. That definition drives the remove-on-completion options below. QUEUED and PENDING are not complete, and neither is ERROR.</p><h2 id="the-processing-path" tabindex="-1">The processing path <a class="header-anchor" href="#the-processing-path" aria-label="Permalink to &quot;The processing path&quot;">​</a></h2><h3 id="once-per-message-on-the-way-in" tabindex="-1">Once per message, on the way in <a class="header-anchor" href="#once-per-message-on-the-way-in" aria-label="Permalink to &quot;Once per message, on the way in&quot;">​</a></h3><ol><li>The source connector receives data. If batch processing is on, the batch splitter produces one message at a time and the rest of this list repeats for each.</li><li>The attachment handler, if configured, extracts attachments and stores them.</li><li>The message row and the source connector message are written, with the source map. What remains after attachment extraction is stored as <strong>Raw</strong>.</li><li>The engine commits. With the source queue on, it responds to the sender here and a source queue thread continues later. With the source queue off, the receiving thread continues at once.</li><li>The preprocessor script runs. Its output becomes <strong>Processed Raw</strong>.</li><li>The source filter and transformer run. The inbound data type serializes the content to its internal form, which is stored as <strong>Transformed</strong>; the filter accepts or rejects; the transformer steps run; the outbound data type produces <strong>Encoded</strong>.</li><li>The source connector message&#39;s status, custom metadata columns, and maps are written. A filtered or errored message skips to the final steps.</li><li>Processed Raw, Transformed, and Encoded are written, subject to the storage mode.</li><li>One connector message is created for the first destination in each chain, each holding a copy of the source&#39;s channel map and response map. The chains then run: all chains at the same time, each chain&#39;s destinations one after another. See <a href="./threading_and_ordering.html">Threading and Ordering</a>.</li></ol><div class="info custom-block"><p class="custom-block-title">INFO</p><p>A connector with no filter and no transformer has no Transformed content. Its Encoded content is the raw content, unchanged apart from any adjustment the data type makes without serializing. This shortcut applies only when the inbound and outbound data types are the same.</p></div><h3 id="for-each-destination" tabindex="-1">For each destination <a class="header-anchor" href="#for-each-destination" aria-label="Permalink to &quot;For each destination&quot;">​</a></h3><ol start="10"><li>The destination&#39;s filter and transformer run against the source&#39;s Encoded content, which is this destination&#39;s Raw. The results are stored as <strong>Transformed</strong> and <strong>Encoded</strong>, the custom metadata columns are updated, and the status becomes QUEUED, FILTERED, or ERROR.</li><li>If the message will be sent now rather than queued, the destination builds its outgoing content from the encoded content and the maps and stores it as <strong>Sent</strong> before attempting the send.</li><li>The send runs, with the pre-queue retries described in <a href="./queueing.html">Queueing</a>. The reply is stored as <strong>Response</strong>.</li><li>If a response transformer is configured, the status becomes PENDING and the engine commits. The response transformer then runs, producing <strong>Response Transformed</strong> and <strong>Processed Response</strong>.</li><li>The custom metadata columns and maps are updated again, and the final status is written: SENT, ERROR, QUEUED, or FILTERED.</li><li>If another destination follows in the chain, its connector message is created with a copy of this destination&#39;s channel map and response map, and the chain continues from step 10.</li></ol><h3 id="once-per-message-on-the-way-out" tabindex="-1">Once per message, on the way out <a class="header-anchor" href="#once-per-message-on-the-way-out" aria-label="Permalink to &quot;Once per message, on the way out&quot;">​</a></h3><ol start="16"><li>When every chain has finished, the postprocessor script runs against a merged view of all connector messages.</li><li>Every destination&#39;s response and the postprocessor&#39;s return value are merged into the source connector message&#39;s response map, and the message row is marked processed.</li><li>Content is removed if the channel&#39;s remove-on-completion options apply.</li><li>The response to send back is selected according to the source connector&#39;s <strong>Response</strong> setting, and the source connector delivers it. The response actually returned is stored as the source&#39;s <strong>Response</strong>.</li></ol><p>When the source queue is on, steps 16 to 18 still happen, on the source queue thread, but step 19 happened back at step 4.</p><h2 id="content-types" tabindex="-1">Content types <a class="header-anchor" href="#content-types" aria-label="Permalink to &quot;Content types&quot;">​</a></h2><h3 id="source-connector" tabindex="-1">Source connector <a class="header-anchor" href="#source-connector" aria-label="Permalink to &quot;Source connector&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Content</th><th>What it holds</th></tr></thead><tbody><tr><td><strong>Raw</strong></td><td>The inbound message after attachment extraction, before the preprocessor</td></tr><tr><td><strong>Processed Raw</strong></td><td>The preprocessor&#39;s output</td></tr><tr><td><strong>Transformed</strong></td><td>The internal representation, usually XML, produced by the inbound data type</td></tr><tr><td><strong>Encoded</strong></td><td>The outbound form produced by the outbound data type after the transformer</td></tr><tr><td><strong>Response</strong></td><td>What was returned to the sender</td></tr></tbody></table><h3 id="destination-connector" tabindex="-1">Destination connector <a class="header-anchor" href="#destination-connector" aria-label="Permalink to &quot;Destination connector&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Content</th><th>What it holds</th></tr></thead><tbody><tr><td><strong>Raw</strong></td><td>Always the source&#39;s Encoded content. Not stored separately</td></tr><tr><td><strong>Transformed</strong></td><td>The internal representation produced by the destination&#39;s inbound data type</td></tr><tr><td><strong>Encoded</strong></td><td>The destination&#39;s outbound form after its transformer</td></tr><tr><td><strong>Sent</strong></td><td>The connector&#39;s outgoing content as built for the send, including its properties, written before the send</td></tr><tr><td><strong>Response</strong></td><td>The reply from the external system</td></tr><tr><td><strong>Response Transformed</strong></td><td>The response in its internal representation</td></tr><tr><td><strong>Processed Response</strong></td><td>The response after the response transformer and the response outbound data type</td></tr></tbody></table><p>Every destination starts from the same source Encoded content. Destinations do not receive each other&#39;s output. What passes from one destination to the next within a chain is the channel map and response map, not the message content.</p><h3 id="maps-and-errors" tabindex="-1">Maps and errors <a class="header-anchor" href="#maps-and-errors" aria-label="Permalink to &quot;Maps and errors&quot;">​</a></h3><p>The maps and errors are stored alongside the content, which is why they remain visible in the Message Browser long after processing finished.</p><table tabindex="0"><thead><tr><th>Content</th><th>What it holds</th></tr></thead><tbody><tr><td><strong>Source Map</strong></td><td>The source map for the message, shared by every connector message</td></tr><tr><td><strong>Connector Map</strong></td><td>This connector message&#39;s connector map</td></tr><tr><td><strong>Channel Map</strong></td><td>The channel map as it stood for this connector message</td></tr><tr><td><strong>Response Map</strong></td><td>Responses keyed by destination, as <code>d1</code>, <code>d2</code>, and so on</td></tr><tr><td><strong>Processing Error</strong></td><td>An error from the filter, transformer, or send</td></tr><tr><td><strong>Postprocessor Error</strong></td><td>An error from the postprocessor script</td></tr><tr><td><strong>Response Error</strong></td><td>An error while handling a response</td></tr></tbody></table><p>The three error types are stored separately, so an error in the postprocessor is distinguishable from one in a transformer.</p><h2 id="message-storage-modes" tabindex="-1">Message storage modes <a class="header-anchor" href="#message-storage-modes" aria-label="Permalink to &quot;Message storage modes&quot;">​</a></h2><p>The <strong>Message Storage</strong> slider on the Summary tab selects one of five modes. The mode is a set of flags that decide what the engine writes and how it commits.</p><table tabindex="0"><thead><tr><th></th><th>Development</th><th>Production</th><th>Raw</th><th>Metadata</th><th>Disabled</th></tr></thead><tbody><tr><td>Raw</td><td>Yes</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td></tr><tr><td>Processed Raw</td><td>Yes</td><td>No</td><td>No</td><td>No</td><td>No</td></tr><tr><td>Transformed</td><td>Yes</td><td>No</td><td>No</td><td>No</td><td>No</td></tr><tr><td>Encoded, source and destination</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td><td>No</td></tr><tr><td>Sent</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td><td>No</td></tr><tr><td>Response</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td><td>No</td></tr><tr><td>Response Transformed, Processed Response</td><td>Yes</td><td>No</td><td>No</td><td>No</td><td>No</td></tr><tr><td>Source response</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td><td>No</td></tr><tr><td>Maps</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td><td>No</td></tr><tr><td>Custom metadata columns</td><td>Yes</td><td>Yes</td><td>Yes</td><td>Yes</td><td>No</td></tr><tr><td>Message and connector message rows</td><td>Yes</td><td>Yes</td><td>Yes</td><td>Yes</td><td>No</td></tr><tr><td>Durable commits</td><td>Yes</td><td>Yes</td><td>Raw write only</td><td>No</td><td>No</td></tr><tr><td>Recovery on start</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td><td>No</td></tr><tr><td>Source queue allowed</td><td>Yes</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td></tr><tr><td>Destination queues allowed</td><td>Yes</td><td>Yes</td><td>No</td><td>No</td><td>No</td></tr></tbody></table><p>Attachments follow the channel&#39;s own attachment setting in every mode except Disabled.</p><p><strong>Durable</strong> means the engine waits for the database to confirm each commit is on disk. When durability is off, the engine asks PostgreSQL for <code>synchronous_commit</code> off and Oracle for a batched no-wait commit on that transaction; on other databases the commit is synchronous regardless. Raw mode keeps the raw write durable and nothing else, which the Administrator describes as &quot;Reprocess only&quot;: after a crash the raw message survives to be reprocessed by hand, but the engine will not resume it.</p><p><strong>Recovery on start</strong> is the engine picking up messages that were mid-flight when it stopped: source messages still in RECEIVED, messages whose destinations never finished, and destinations in PENDING. It runs only in Development and Production. In the other modes the engine logs that incomplete messages exist and skips them.</p><p><strong>Disabled</strong> replaces the storage layer with one that writes nothing. Statistics still update. Nothing else about the message reaches the database, so the Message Browser is empty and neither queueing nor recovery is possible.</p><h2 id="removing-content-on-completion" tabindex="-1">Removing content on completion <a class="header-anchor" href="#removing-content-on-completion" aria-label="Permalink to &quot;Removing content on completion&quot;">​</a></h2><p>Three options on the Summary tab remove stored data the moment a message completes, on the processing thread, in or immediately after the finishing transaction.</p><table tabindex="0"><thead><tr><th>Option</th><th>Effect</th></tr></thead><tbody><tr><td><strong>Remove content on completion</strong></td><td>Deletes every stored content row for the message</td></tr><tr><td><strong>Filtered only</strong></td><td>Restricts the deletion to the connector messages that were filtered. Enabled only with the option above</td></tr><tr><td><strong>Remove attachments on completion</strong></td><td>Deletes the message&#39;s attachments</td></tr></tbody></table><p>Completion is the status test given above: every connector message FILTERED, TRANSFORMED, or SENT. A message with a queued, pending, or errored connector message is never trimmed by these options. The message and connector message rows are never removed by them either, so the message stays searchable in the Message Browser with its metadata and statuses, and only the payload is gone. The options are unavailable in Metadata and Disabled modes, where there is nothing to remove.</p><p>This is not pruning. The <a href="./data_pruning_and_maintenance.html">Data Pruner</a> runs on a schedule, selects by age, and deletes the message rows themselves. The two mechanisms are independent and are usually used together.</p><h2 id="custom-metadata-columns" tabindex="-1">Custom metadata columns <a class="header-anchor" href="#custom-metadata-columns" aria-label="Permalink to &quot;Custom metadata columns&quot;">​</a></h2><p>A custom metadata column is a real column on the channel&#39;s <code>d_mcm</code> table. Each is defined on the Summary tab with a name, a type, and a mapping variable.</p><table tabindex="0"><thead><tr><th>Type</th><th>Stored as</th></tr></thead><tbody><tr><td>STRING</td><td>Text, truncated to 255 characters</td></tr><tr><td>NUMBER</td><td>A number below 10^16</td></tr><tr><td>BOOLEAN</td><td>True or false</td></tr><tr><td>TIMESTAMP</td><td>A date and time</td></tr></tbody></table><p>When a connector message&#39;s metadata is written, the engine looks the mapping variable up in the connector map, then the channel map, then the source map, and casts the value to the column type. A value that cannot be cast is skipped with a warning; it does not fail the message. Column names are stored in upper case. On every deploy the engine reconciles the table with the channel&#39;s definition, adding new columns, dropping removed ones, and recreating a column whose type changed, which discards its existing values.</p><h2 id="attachments" tabindex="-1">Attachments <a class="header-anchor" href="#attachments" aria-label="Permalink to &quot;Attachments&quot;">​</a></h2><p>An attachment is content the attachment handler extracts from the raw message before anything else runs, including the preprocessor. It belongs to the message rather than to any one connector message and is stored once, in <code>d_ma</code>. Because the engine keeps several copies of message content as it moves through a channel, keeping large or binary content out of those copies reduces both memory and storage. When a destination sends, the engine splices the attachment back into the outgoing content.</p><h2 id="batch-processing" tabindex="-1">Batch processing <a class="header-anchor" href="#batch-processing" aria-label="Permalink to &quot;Batch processing&quot;">​</a></h2><p>The batch processor lets a source connector split one inbound payload into many messages. With a streaming-capable connector, such as the File Reader or a TCP Listener using MLLP, a very large payload is read incrementally rather than loaded whole.</p><p>Turn it on with <strong>Process Batch</strong> in the source connector&#39;s settings. How the payload is split is controlled by the batch properties of the source inbound data type. Messages from one payload are processed one after another on the receiving thread, whatever the channel&#39;s thread settings.</p><p>Three source map variables are populated for batch messages:</p><ul><li><strong>batchId</strong>. Shared by every message from the same payload.</li><li><strong>batchSequenceId</strong>. Counts up from 1 within the payload.</li><li><strong>batchComplete</strong>. True only for the last message.</li></ul><p>The source connector&#39;s <strong>Batch Response</strong> setting chooses whether the first or the last message&#39;s response is returned to the sender.</p>',58)]))}const g=t(a,[["render",r]]);export{p as __pageData,g as default};
